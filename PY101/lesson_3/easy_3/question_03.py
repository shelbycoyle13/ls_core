# What will the following code output? Try to answer without running the code.

str1 = "hello there" #str1 points to this string
str2 = str1 #str2 points to the object referenced by str1, "hello there"
str2 = "goodbye!" # We reassign str2 to a different string
print(str1) #This prints "hello there"

