# Will the code below raise an error?

numbers = [1, 2, 3]
numbers[6] = 5

# Yes, this will raise an error because we are trying to reach an index that doesn't exist in the numbers list. Specifically, it will give an out of range IndexError.